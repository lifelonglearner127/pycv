Face Detection on Browser
=========================

- Install Nginx
- Create Virtual Host
- Copy the `javascript` folder to documentation root
- getUserMedia() can only be called from an HTTPS URL, localhost or a file:// URL.
